# TypeScript Type Declarations

Ambient type declarations for third-party modules without built-in types.

## Files

- `wapalyzer-core.d.ts` — Type declarations for `wapalyzer-core` npm package

## Conventions

- Only module augmentation and ambient declarations
- Application types are co-located with their components/utilities
- Use `declare module` for third-party package types
