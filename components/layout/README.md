# Layout Components

Shared layout components rendered by the root layout (`app/layout.tsx`) and page layouts.

## Files

- `header.tsx` — Sticky header with logo and theme toggle (server component)
- `footer.tsx` — Site footer with branding (server component)
- `theme-toggle.tsx` — Dark/light theme switcher (client component, uses localStorage + prefers-color-scheme)
