# Public Assets

Static assets served by Next.js at the root path (`/`).

## Structure

```
public/
└── screenshots/   — Captured website screenshots (PNG, auto-generated, gitignored)
```

Screenshots are generated by `app/api/capture/route.ts` using Playwright and stored with hashed filenames to prevent enumeration.
