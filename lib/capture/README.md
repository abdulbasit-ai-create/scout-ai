# Capture Module

Reserved for capture-related utilities extracted from `app/api/capture/route.ts` in future refactoring.

Currently, capture logic lives in the API route handler. This directory can hold helpers for URL normalization, validation, and screenshot management as the codebase grows.
